# Coding 面试 Checklist：完整流程、约束与解法、常见注意事项

适用于算法 coding 面试和刷题群模拟面试。根据题目选择相关检查项，不必逐条机械询问。

## 1. Before Coding：理解题目、确认约束、讨论方案

- [ ] **用自己的话确认目标**：输入是什么，返回值、下标、数量、路径，还是原地修改？ <!-- task:check-001 -->
- [ ] 确认要一个答案还是所有答案，是否要求顺序或去重。 <!-- task:check-002 -->
- [ ] **确认关键约束**：输入规模、数值范围、是否有序、是否重复、是否包含负数。 <!-- task:check-003 -->
- [ ] 确认能否修改输入、使用额外空间，以及是否必须保留原下标或顺序。 <!-- task:check-004 -->
- [ ] 确认是否保证有解，无解时返回什么。 <!-- task:check-005 -->
- [ ] 确认是单次查询还是多次查询，查询期间是否存在更新。 <!-- task:check-006 -->
- [ ] 根据题型确认区间端点、字符集、图的方向与边权等。 <!-- task:check-007 -->
- [ ] **确认关键边界**：空输入是否有效，单元素、零值、重复值和相等情况如何处理。 <!-- task:check-008 -->
- [ ] 必要时用一个能暴露歧义的小例子确认预期输出。 <!-- task:check-009 -->
- [ ] **解释解法**：如有帮助，简述直接解法及复杂度，指出瓶颈，再解释优化方式。 <!-- task:check-010 -->
- [ ] 说明数据结构的作用和算法的主要步骤。 <!-- task:check-011 -->
- [ ] **解释正确性**：状态或不变量是什么？为什么不会漏答案？为什么可以移动指针、剪枝或丢弃候选？ <!-- task:check-012 -->
- [ ] 定义 `n`、`m` 等变量，给出时间和辅助空间复杂度。 <!-- task:check-013 -->
- [ ] 简单确认方案，对方没有异议即可开始实现。 <!-- task:check-014 -->

### 注意事项

- 不必强行给两种解法，也不必先实现暴力解。
- 已经明确的约束不要重复询问；只问会影响理解或实现的条件。
- 不确定的关键条件要问；次要条件可以说明假设。
- 不能只说“用滑动窗口”，要解释窗口维护什么、何时收缩。
- 写代码前说明算法为什么成立，而不只是报出算法名称。
- 不必反复请求许可或等待正式的 “go ahead”。

## 2. 约束与解法的对应关系

**输入约束主要影响算法选择，边界情况主要用于检查实现和验证假设。**

| 约束 / 条件 | 对解法的影响 | 注意事项 |
| --- | --- | --- |
| 输入规模 | 判断暴力、枚举、排序或线性算法是否可行 | 同时考虑多组输入总规模和输出规模 |
| 数组有序 | 可能使用二分、左右双指针 | 仍需解释为什么可以排除候选 |
| 数组无序 | 考虑哈希或先排序 | 排序有成本，可能破坏原顺序 |
| 必须保留原下标 | 哈希，或排序时保存 `(value, index)` | 排序后的下标不等于原下标 |
| 不允许修改输入 | 不能直接原地排序、原地标记 | 复制输入需要额外空间 |
| 存在重复值 | 可能需要频次表和去重规则 | set 不能表达出现数量 |
| 元素非负 | 某些总和阈值问题可使用滑动窗口 | 扩张时总和不下降，收缩时不增加 |
| 允许负数 | 基于总和单调性的窗口可能失效 | 根据目标考虑前缀和 + 哈希、单调队列等 |
| 统计和等于 `k` 的连续子数组数量 | 前缀和 + 频次哈希表 | 保存历史前缀和的次数，而不只是存在性 |
| 小整数值域 | 计数数组、桶、差分数组 | 空间取决于值域，不只是元素个数 |
| 大值域或稀疏坐标 | 哈希、排序、坐标压缩 | 坐标压缩保留顺序，不自动保留实际距离 |
| 固定小字符集 | 数组计数 | 一般字符集可使用字典 |
| 单次查询 | 直接扫描可能足够 | 不一定值得建立复杂结构 |
| 静态数据、多次区间求和 | 前缀和 | 预处理 `O(n)`，每次查询 `O(1)` |
| 点更新 + 区间求和 | Fenwick tree 或线段树 | 普通前缀和更新成本较高 |
| 数据流或内存有限 | 在线处理、有界堆等 | 不能默认保存并排序全部数据 |
| 返回所有答案 | 枚举、去重，计算输出成本 | 答案数量可能达到平方级或指数级 |
| 二分答案 | 需要单调的可行性判定 | 不能仅因为答案是数字就二分 |
| 图中边代价相同且非负 | BFS 可求最短路径 | 最少边数对应最小总代价 |
| 图中边权非负 | 可用 Dijkstra | 普通 BFS 不适用于一般带权最短路 |
| 图中有负权边 | 考虑 Bellman–Ford 等适用算法 | 还需考虑相关负环是否使最短路无定义 |
| 图中可能有环 | visited 或状态标记 | 有向图判环需区分正在访问与已完成 |
| 二叉搜索树 | 可利用大小关系剪枝 | 普通二叉树不具有该保证 |
| 连续子数组与不连续子序列 | 候选结构不同，解法通常不同 | 前者常考虑窗口、前缀和；后者常考虑 DP、贪心等 |

> 包含负数不代表所有滑动窗口都失效。例如“最多包含 k 种不同元素”的窗口不依赖数值正负。算法选择仍取决于完整问题。

### 常见输入规模与复杂度参考

以下是面试和刷题时用于筛选方案的**经验参考，不是通过时限的保证**。默认 `n` 是主要输入规模；具体还取决于时间限制、语言、常数、数据结构、测试组数以及每一步的实际成本。

| 常见输入规模 | 优先考虑的复杂度 | 常见候选解法 | 判断重点 |
| --- | --- | --- | --- |
| `n ≤ 8～10` | `O(n!)` 有时可行 | 全排列、排列型回溯、暴力搜索 | `10! ≈ 363 万`；若每个排列还要扫描或复制，需再乘 `n` |
| `n ≤ 20` 左右 | `O(2ⁿ)`；`O(n·2ⁿ)` 需评估 | 子集枚举、回溯、状压 DP | `2²⁰ ≈ 105 万`；乘 `n` 后约 2100 万，Python 中可能较重 |
| `n ≈ 30～40` | 可考虑 `O(2^(n/2))` 量级 | 折半搜索（meet-in-the-middle） | 原问题需能拆成两半；排序、匹配、存储可能增加成本 |
| `n ≈ 100` | `O(n³)` 可作为候选 | 区间 DP、Floyd–Warshall、三重枚举 | `100³ = 100 万`，仍需看每步成本 |
| `n ≈ 300～500` | 通常优先 `O(n²)`；`O(n³)` 需谨慎 | 二维 DP、两两枚举 | `500³ = 1.25 亿`，纯 Python 三重循环通常风险很高 |
| `n ≈ 1,000～3,000` | `O(n²)` 可能可行 | 二维 DP、两两比较、枚举起终点 | `3,000² = 900 万`；时间和二维表内存都需估算 |
| `n ≈ 10⁴` | 优先 `O(n log n)` 或 `O(n)` | 排序、堆、二分、哈希、双指针 | `n² = 1 亿`，不要默认二次算法能通过 |
| `n ≈ 10⁵～2×10⁵` | 通常 `O(n log n)` 或 `O(n)` | 排序 + 贪心、堆、Fenwick tree、线段树、线性扫描 | 二次算法通常不可行；检查是否有隐藏复制或重复扫描 |
| `n ≈ 10⁶` | 优先 `O(n)`；`O(n log n)` 视情况 | 线性扫描、计数、双指针；高效排序也可能可行 | 内存、对象数量、输入输出和常数开始非常重要 |
| `n ≈ 10⁷` 或更大 | 尽量线性且常数小，或利用结构避免枚举 | 流式处理、数学公式、分块、位运算 | 若输入显式包含 `n` 个元素，读取全部输入本身就需 `Ω(n)` |
| 数值上限为 `10⁹～10¹⁸`，但元素很少 | 按元素数量及数值位数分析；可能用 `O(log U)`、`O(√U)` 等 | 二分答案、快速幂、欧几里得算法、数论方法 | 数值范围 `U` 不等于数组长度；不能据此直接断定需要 `O(log n)` |

**如何使用这张表：** 用约束排除明显不合适的方案，再根据问题结构选择算法。不能看到 `n = 10⁵` 就直接断定要用滑动窗口或贪心。

### 多维输入：不要只看一个 n

| 输入结构 | 应估算的总成本 | 示例 |
| --- | --- | --- |
| `m × n` 矩阵 | 单次完整遍历为 `O(mn)` | `1,000 × 1,000` 有 100 万个格子 |
| `V` 个点、`E` 条边的图 | 邻接表 BFS / DFS 为 `O(V + E)` | 稠密图中 `E` 可达 `O(V²)`；邻接矩阵空间为 `O(V²)` |
| 两个字符串长度为 `m`、`n` | 常见二维 DP 为 `O(mn)` | 两者各长 5,000 时，有 2500 万个状态 |
| `q` 次查询，每次扫描 `n` 个元素 | 总时间 `O(qn)` | `q = n = 10⁵` 时约 100 亿次元素访问，需要考虑预处理 |
| 容量为 `W` 的 0/1 背包 | 经典 DP 为 `O(nW)` | 不只看物品数；`n = 100`、`W = 10⁹` 时无法直接建容量表 |
| `T` 组测试 | 对各组实际成本求和 | 二次算法为 `Σ nᵢ²`；检查题目是否限制总输入规模 |
| 字符串作为哈希键 | 还需考虑字符串长度、构造及哈希成本 | 每轮切出长度 `k` 的子串，不能把整个操作当成 `O(1)` |
| 输出所有组合或排列 | 计入生成、复制和存储输出的成本 | 显式返回全部子集，最坏总输出大小为 `Θ(n·2ⁿ)` |

### 时间可行不代表空间可行

- `O(n²)` 时间有时可接受，但保存完整 `n × n` 表可能超内存。检查能否滚动数组或按需计算。
- Python 的 list、dict、set 和整数对象有额外开销，不能简单把每个元素都按 4 字节计算。
- 递归深度受运行环境限制：即使总时间为 `O(n)`，长链结构仍可能使递归过深。
- 不要把“每秒能执行固定多少次操作”当作硬规则；Python 循环、内置排序、哈希和字符串复制的成本差别很大。
- 用数值范围推导算法时，还要检查溢出和大整数操作成本。

### 面试中的判断示例

> “With up to 100,000 elements, a quadratic approach would require on the order of ten billion operations. I’d look for an O(n log n) or O(n) solution.”

> “There are only 20 elements, so enumerating subsets may be feasible. I’ll also account for the work done per subset and the memory needed.”

约束检查可额外勾选：

- [ ] 区分元素个数、数值上限、维度和查询次数。 <!-- task:check-015 -->
- [ ] 估算候选算法在最大输入下的操作数量。 <!-- task:check-016 -->
- [ ] 计入每个状态的转移、扫描、复制和输出成本。 <!-- task:check-017 -->
- [ ] 同时评估时间与空间，不仅凭复杂度符号判断。 <!-- task:check-018 -->

### 面试表达示例

> “Can the array contain negative numbers? If all values are nonnegative, expanding the window never decreases the sum, which lets us use a sliding window for this threshold condition. With negative values, that reasoning would no longer hold.”

> “Will the array change between queries? If it stays unchanged, I can build prefix sums for constant-time range-sum queries. If updates are required, I’d consider a Fenwick tree.”

## 3. During Coding：清晰实现、解释关键决策

- [ ] 明确函数签名、参数和返回值。 <!-- task:check-019 -->
- [ ] 使用有意义的命名，保持代码结构直观。 <!-- task:check-020 -->
- [ ] 实现与之前解释的方案一致。 <!-- task:check-021 -->
- [ ] 正确初始化答案、指针、计数器和状态。 <!-- task:check-022 -->
- [ ] 在访问数据之前完成必要边界判断。 <!-- task:check-023 -->
- [ ] 确保状态更新顺序正确。 <!-- task:check-024 -->
- [ ] 确保循环推进，递归有明确终止条件。 <!-- task:check-025 -->
- [ ] 必要时拆 helper，避免过度设计。 <!-- task:check-026 -->
- [ ] 解释关键决策、状态变化和不变量。 <!-- task:check-027 -->
- [ ] 如果改变方案，先说明原因，再修改代码。 <!-- task:check-028 -->

### 注意事项

- **解释逻辑，不逐行念代码。**
- 可以短暂安静思考，不需要一直说话。
- 卡住时说明具体困难和正在验证的假设。
- 接到提示后，解释如何把提示融入方案。
- 优先写容易理解和验证的代码，不追求最短写法。

例如：

> “The window is invalid now, so I’ll move the left boundary until it satisfies the constraint again.”

> “Let me think through the boundary condition for a moment.”

## 4. After Coding：自查、走例子、测试、修复

- [ ] **快速自查**：下标、循环范围、比较符号、初始化、更新顺序。 <!-- task:check-029 -->
- [ ] 检查返回值、无解情况和循环结束后的收尾。 <!-- task:check-030 -->
- [ ] 检查是否错误地重复使用同一个元素。 <!-- task:check-031 -->
- [ ] **按实际代码走一个正常例子**，追踪关键变量，覆盖主要分支。 <!-- task:check-032 -->
- [ ] 核对实际结果与预期结果。 <!-- task:check-033 -->
- [ ] **测试针对性边界**：最小合法输入、重复值、零值、无解、答案在末尾等。 <!-- task:check-034 -->
- [ ] **发现 bug 后闭环**：失败用例 → 根因 → 修复 → 重测。 <!-- task:check-035 -->
- [ ] 必要时检查受修改影响的其他分支。 <!-- task:check-036 -->
- [ ] 有运行环境时执行测试，先确定预期结果再运行。 <!-- task:check-037 -->

### 注意事项

- 手动走代码不是重新讲一遍算法。
- 不要只测试题目给的例子。
- 修改后先判断为何有效，不要无依据地反复调整条件。
- 测试通过不代替正确性和复杂度分析。
- 不必测试所有边界，优先选择能暴露当前实现问题的用例。

## 5. 边界情况与检查重点

| 边界情况 | 重点检查 |
| --- | --- |
| 空输入 | 是否访问 `nums[0]`、`matrix[0]`，或对空集合求极值；仅在允许空输入时处理 |
| 单元素 | 循环不执行时，初始化和返回值是否正确 |
| 两个元素 | 指针相遇、最后一轮比较是否正确 |
| 重复值 / 全相同 | 是否丢失计数、重复输出或错误处理相等情况 |
| 零值 | 是否把 `0` 错当成不存在，是否可能除零 |
| 无解 | 返回约定值，而不是未更新的哨兵值 |
| 答案在开头 | 初始化是否遗漏起始位置 |
| 答案在末尾 | 是否遗漏循环结束后的收尾 |
| 全部满足 / 全不满足 | 整体答案、空结果和收缩逻辑是否正确 |
| 区间端点相等 | 闭区间 `[1,2]` 与 `[2,3]` 相交，半开区间 `[1,2)` 与 `[2,3)` 不相交 |
| 相同优先级 | 是否需要第二排序字段，例如房间编号 |
| 单行 / 单列矩阵 | 转向后是否重复访问 |
| 极端数值 | 溢出、哨兵值、比较是否安全 |
| 极深树 / 长链图 | 递归深度可能过大，是否需要显式栈 |
| 不连通图 | 是否需要从所有未访问节点启动遍历 |

## 6. 不同算法的专项检查

| 算法 | 必须说清楚 | 最容易出错的地方 |
| --- | --- | --- |
| 双指针 | 每次移动为什么不会漏答案 | 移动方向、相等条件、重复使用元素 |
| 滑动窗口 | 窗口含义、合法条件、何时更新答案 | 收缩时计数不同步，误用 `if` / `while` |
| 二分 | 搜索区间定义、排除规则 | 边界不一致、区间不缩小、死循环 |
| 前缀和 + 哈希 | 查询的历史状态是什么 | 初始前缀和、查询与插入顺序、次数与存在性 |
| DFS / 回溯 | 当前层的选择、终止条件 | 忘记恢复状态、结果未复制 |
| BFS | 队列含义、分层或距离规则 | 普通 BFS 中 visited 标记过晚导致重复入队 |
| 堆 | 堆顶含义、何时入堆出堆 | 空堆访问、排序字段、同优先级规则 |
| DP | 状态、转移、初始值、计算顺序 | 下标、不可达状态、空间压缩后覆盖旧值 |
| 贪心 | 当前选择为什么安全 | 只有直觉，没有正确性依据 |
| 区间 | 排序依据、重叠定义 | 端点相等、最后一个区间未加入结果 |
| 链表 | 修改前后指针关系 | 丢失后续节点、空指针、意外成环 |

## 7. 收尾与 Follow-up

- [ ] 按最终代码确认时间复杂度。 <!-- task:check-038 -->
- [ ] 确认辅助空间，包含递归栈、切片、复制、排序等。 <!-- task:check-039 -->
- [ ] 必要时单独说明输出空间。 <!-- task:check-040 -->
- [ ] 说明是否修改输入。 <!-- task:check-041 -->
- [ ] 如果还有有意义的优化，解释收益与代价。 <!-- task:check-042 -->
- [ ] Follow-up 先确认新要求，再分析原方案哪里受影响。 <!-- task:check-043 -->
- [ ] 说明调整方案和复杂度，然后按要求修改。 <!-- task:check-044 -->

### 复杂度注意事项

- 嵌套循环不一定是 `O(n²)`，要计算总操作次数。
- 递归栈空间取决于最大同时存在的调用深度，还要计入每层保存的数据。
- Python 切片和复制会分配额外空间。
- 原地排序不代表排序过程只使用 `O(1)` 辅助空间。
- 哈希操作通常按平均 `O(1)` 分析，要明确分析口径。
- 返回全部结果时不能忽略输出数量。
- 已经达到合理的最优复杂度时，解释理由即可，不必强行继续优化。

### 常见 Follow-up

- 输入变成数据流，或无法全部放入内存。
- 同一份数据要回答很多次查询。
- 查询期间允许更新。
- 不允许修改输入，或限制额外空间。
- 要求返回所有答案。
- 输入已排序，或值域很小。

处理顺序：**确认变化 → 分析原方案的影响 → 提出调整及取舍 → 按要求修改。**

## 8. 模拟面试精简 Checklist

### Before Coding

- [ ] 复述目标，明确输入和输出。 <!-- task:check-045 -->
- [ ] 确认影响解法的约束，并根据最大输入规模筛选可行复杂度。 <!-- task:check-046 -->
- [ ] 确认关键边界与相等规则。 <!-- task:check-047 -->
- [ ] 必要时举例确认理解。 <!-- task:check-048 -->
- [ ] 简述直接解法与瓶颈。 <!-- task:check-049 -->
- [ ] 解释优化方案与数据结构。 <!-- task:check-050 -->
- [ ] 说明正确性 / 不变量。 <!-- task:check-051 -->
- [ ] 给出时间与辅助空间复杂度。 <!-- task:check-052 -->
- [ ] 简单确认方案，开始实现。 <!-- task:check-053 -->

### During Coding

- [ ] 命名清晰、结构直观。 <!-- task:check-054 -->
- [ ] 解释关键逻辑与决策。 <!-- task:check-055 -->
- [ ] 初始化、边界、更新顺序正确。 <!-- task:check-056 -->
- [ ] 循环推进、递归终止。 <!-- task:check-057 -->
- [ ] 改变方案或卡住时说明思考。 <!-- task:check-058 -->

### After Coding

- [ ] 自查下标、条件、返回与收尾。 <!-- task:check-059 -->
- [ ] 按实际代码走一个正常例子。 <!-- task:check-060 -->
- [ ] 测试最小输入和针对性边界。 <!-- task:check-061 -->
- [ ] 定位 bug → 修复 → 重测。 <!-- task:check-062 -->
- [ ] 确认最终复杂度和输入修改情况。 <!-- task:check-063 -->
- [ ] 讨论有意义的优化与取舍。 <!-- task:check-064 -->
- [ ] Follow-up：确认变化 → 分析 → 修改。 <!-- task:check-065 -->

## 9. 时间安排与复盘

如果一道主要题目有约 30 分钟，可以参考以下安排，按难度灵活调整：

| 阶段 | 参考时间 |
| --- | --- |
| 理解题目与约束 | 3 分钟 |
| 方案、正确性与复杂度 | 4 分钟 |
| 实现 | 15 分钟 |
| 验证与修复 | 5 分钟 |
| 收尾与追问 | 3 分钟 |

若有多道题，需要相应压缩每题讨论，但仍应主动留出验证时间。

复盘可从以下维度记录，不作为任何公司的官方评分标准：

- **Problem Solving**：能否理解问题、识别约束、推导方案。
- **Correctness**：算法和实现是否正确，是否解释关键依据。
- **Communication**：表达是否清楚，能否有效回应提示。
- **DSA & Complexity**：数据结构选择和复杂度分析是否准确。
- **Code Quality**：命名、结构、可读性和可维护性。
- **Testing & Debugging**：测试是否有针对性，能否定位和修复问题。
- **Time Management**：能否合理分配讨论、实现和验证时间。

练习优先做到：**思路可解释、代码正确、验证充分，然后提高速度。**
